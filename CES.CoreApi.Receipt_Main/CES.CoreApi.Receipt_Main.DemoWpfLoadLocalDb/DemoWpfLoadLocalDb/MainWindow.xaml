<Window x:Class="WpfLocalDb.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLocalDb"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <StackPanel>
        <!--Cargar Documentos-->
        <TextBlock  Text="Descargar Documentos" FontWeight="Bold" Margin="0 30 10 10"/>
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Left" Margin="0 10 0 0 ">
            <Button Content="Descargar Documentos" Click="DescargarDocumentos_Click" Padding="2" Margin="10 10 0 10"/>
        </StackPanel>
        
        <Grid Margin="20">
            <ProgressBar Minimum="0" Maximum="100" Name="pbStatus" Margin="10 10 0 10" Padding="10" Height="20"/>
            <TextBlock Text="{Binding ElementName=pbStatus, Path=Value, StringFormat={}{0:0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <DataGrid x:Name="DocumentsList" AutoGenerateColumns="True" ></DataGrid>
    </StackPanel>  
</Window>
